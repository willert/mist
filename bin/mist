#!/usr/bin/env perl

use FindBin qw/$Bin/;
use lib "$Bin/../lib";

# MAC seems to cause problems if not pre-loaded here
use MooseX::App::Cmd;
use MooseX::App::Cmd::Command;

use App::Mist;
App::Mist->run;
